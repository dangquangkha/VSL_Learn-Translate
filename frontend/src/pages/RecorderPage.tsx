import { AppShell } from '../components/layout/AppShell'

// Stub page — P3 (An) sẽ implement Recorder + thu thập dữ liệu

export default function RecorderPage() {
  return (
    <AppShell>
      <div style={{ textAlign: 'center', padding: '80px 16px' }}>
        <div style={{ fontSize: 48, marginBottom: 16 }}>🎥</div>
        <h1 style={{ fontSize: 24, fontWeight: 700, color: '#222', margin: '0 0 8px' }}>
          Đóng góp dữ liệu (Recorder)
        </h1>
        <p style={{ color: '#888', fontSize: 15 }}>
          Đang xây dựng... P3 (An) đang triển khai tính năng này.
        </p>
      </div>
    </AppShell>
  )
}
